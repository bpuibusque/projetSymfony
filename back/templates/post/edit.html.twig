{% extends 'base.html.twig' %}

{% block title %}Edit Post{% endblock %}

{% block body %}
    <h1>Edit Post</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">Save changes</button>
    {{ form_end(form) }}
{% endblock %}
